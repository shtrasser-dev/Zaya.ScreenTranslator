# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
History starts at the current release line; older releases are not backfilled.

## [Unreleased]

### Added

- Overlay: snap nearly-horizontal lines to the horizon (default `10` degrees).

### Changed

- GitHub Release notes are taken from this file’s `[Unreleased]` section (manual changelog input removed from Publish).
- Main window: min width `780`, flexible centered fields column, labels above inputs, settings toggle sharing a row with status.
- Settings UI: equal column width across tabs, engine picker beside each module intro, no trailing divider under the last setting, overlay setting labels without `(%)` / `(px)` and clearer unit descriptions.
- Capture regions editor opens without a selected window when regions are already configured (transparent `800×800` placeholder).

## [1.1.3] - 2026-08-11

### Added

- Main-window theme toggle.

### Changed

- Soften sun emoji opacity on the theme toggle.
